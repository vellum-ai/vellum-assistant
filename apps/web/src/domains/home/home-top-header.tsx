/**
 * Top-level page header for the home dashboard: the "Overview" title. Sits
 * above the Schedules / Notifications tabs. Mirrors the Library page header
 * styling.
 */
export function HomeTopHeader() {
  return (
    <h1 className="shrink-0 text-title-large text-[var(--content-default)]">
      Overview
    </h1>
  );
}
