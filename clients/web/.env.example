# Backend target for Vite's development proxy.
# Not embedded in the client bundle (no VITE_ prefix).
# Default: http://localhost:8000
#
# Reference: https://vite.dev/config/server-options#server-proxy
# API_PROXY_TARGET=http://localhost:8000

# Dev server port. Default: 3000
# PORT=3000

# Set to enable platform (cloud-hosted) mode. When unset, the app defaults to local mode,
# reading the assistant lockfile from the CLI daemon via /__local/lockfile.
# VITE_PLATFORM_MODE=true

# Sentry DSNs are build-time, host-selected: the shared web bundle picks one per host.
#   web → VITE_SENTRY_DSN (vellum-assistant-web),
#   macOS Electron renderer → VITE_SENTRY_DSN_MACOS (vellum-assistant-macos),
#   Windows Electron renderer → VITE_SENTRY_DSN_WINDOWS (vellum-assistant-windows),
#   iOS → VITE_SENTRY_DSN_IOS (vellum-assistant-ios, baked into the SPA build),
#   Android → VITE_SENTRY_DSN_ANDROID (vellum-assistant-android, baked into the SPA build).
# Injected by CI/CD; leave unset locally (Sentry no-ops on an empty DSN).
# VITE_SENTRY_DSN=
# VITE_SENTRY_DSN_MACOS=
# VITE_SENTRY_DSN_WINDOWS=
# VITE_SENTRY_DSN_IOS=
# VITE_SENTRY_DSN_ANDROID=

# Live Google Play listing for ai.vellum.assistant. Leave unset until the
# listing is reachable so Android install promotion remains disabled.
# VITE_ANDROID_PLAY_STORE_URL=https://play.google.com/store/apps/details?id=ai.vellum.assistant
