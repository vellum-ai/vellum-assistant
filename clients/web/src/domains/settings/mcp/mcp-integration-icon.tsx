import { Puzzle } from "lucide-react";

export function McpIntegrationIcon() {
  return (
    <span
      aria-hidden="true"
      className="flex size-8 shrink-0 items-center justify-center rounded-md bg-[var(--surface-base)] text-[var(--content-secondary)]"
    >
      <Puzzle size={20} />
    </span>
  );
}
