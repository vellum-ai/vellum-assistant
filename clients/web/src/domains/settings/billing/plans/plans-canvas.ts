/**
 * The View Plans takeover's near-black canvas, shared with its stories. No
 * surface token holds this value: the darkest dark-theme surface is
 * `--surface-base` (#17191C), so the raw hex stands.
 */
export const PAGE_BACKGROUND = "#0A0A0B";
