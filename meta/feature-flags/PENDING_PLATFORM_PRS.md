# Pending Platform PRs for Feature Flags

This file tracks assistant feature flags that have been declared in
`feature-flag-registry.json` but do not yet have a corresponding
Terraform entry in the `vellum-assistant-platform` repo. Per
`CLAUDE.md` (see the Assistant Feature Flags section) and
`meta/feature-flags/AGENTS.md`, a new flag in this registry requires a
companion PR in `vellum-assistant-platform` to provision the flag on the
platform for remote sync.

Remove an entry from this file once its companion platform PR is merged.

## Open entries

| Flag key                                                  | Registry declaration date | Owner                    | Status                                                                                                                                                            | Required platform work                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------------------------------- | ------------------------- | ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `meet`                                                    | 2026-04-19                | meet owner               | Platform PR not yet opened (as of 2026-04-19)                                                                                                                     | Terraform entry in `../vellum-assistant-platform/terraform/launchdarkly.tf` (or equivalent) with `defaultEnabled: false` and a description pointing at `skills/meet-join/SKILL.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `experiment-activation-flow-2026-06-03` (`variant-b` arm) | 2026-06-11                | activation owner         | Platform PR not yet opened (as of 2026-06-11)                                                                                                                     | Flag already exists in LaunchDarkly; add the `variant-b` string variation (preseeded personal-page app) in `vellum-assistant-platform` terraform, and serve the flag to assistant-scope consumers now that the registry scope changed `client` → `both`.                                                                                                                                                                                                                                                                                                                                                                                                     |
| `channel-trust-floors`                                    | 2026-06-17                | admission policy owner   | Platform PR not yet opened (as of 2026-06-17)                                                                                                                     | Terraform entry in `../vellum-assistant-platform/terraform/launchdarkly.tf` (or equivalent) with `defaultEnabled: true` (registry default flipped on 2026-07-10) provisioning the `channel-trust-floors` assistant flag, which gates the Channel Trust Floors (per-channel admission policy) Privacy settings card.                                                                                                                                                                                                                                                                                                                                          |
| `mcp-add-server`                                          | 2026-06-23                | MCP settings owner       | Platform PR not yet opened (as of 2026-06-23)                                                                                                                     | Terraform entry in `../vellum-assistant-platform/terraform/launchdarkly.tf` (or equivalent) with `defaultEnabled: false` provisioning the `mcp-add-server` assistant flag, which gates the Add Server action on the MCP settings page.                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `proactive-tips`                                          | 2026-07-15                | proactive tips owner     | [vellum-assistant-platform#9213](https://github.com/vellum-ai/vellum-assistant-platform/pull/9213) opened 2026-07-16                                              | Terraform entry in `../vellum-assistant-platform/terraform/launchdarkly.tf` (or equivalent) provisioning the client-scoped `proactive-tips` string flag with variations `off`/`on` and default `off`. Gates the dismissible proactive tip card in the web sidebar; string-valued so future A/B arms slot in as new values.                                                                                                                                                                                                                                                                                                                                   |
| `assistant-switcher`                                      | 2026-07-30                | assistant switcher owner | Platform PR not yet opened (as of 2026-07-30)                                                                                                                     | Terraform entry in `../vellum-assistant-platform/terraform/launchdarkly.tf` (or equivalent) with `defaultEnabled: false` provisioning the client-scoped `assistant-switcher` flag, which gates the Switch Assistant section in General settings (local mode only).                                                                                                                                                                                                                                                                                                                                                                                           |
| `assistant-reply-push`                                    | 2026-07-31                | notifications owner      | Platform PR not yet opened (as of 2026-07-31)                                                                                                                     | Terraform entry in `../vellum-assistant-platform/terraform/gcp/env/prod/vellum-assistant/main.tf` with `defaultEnabled: true` and `tags = ["assistant"]` provisioning the `assistant-reply-push` assistant flag, which gates the `chat.assistant_reply` push the daemon emits at the end of a user-initiated turn whose reply the user has not seen. Declared on the unseen-reply-notify feature branch rather than `main`, so this row runs ahead of the registry until that branch lands. Needed as a remote kill switch: until it exists the flag resolves to the registry default (on), and the only way to silence the push is a client-side override.  |
| `desktop-presence-suppression`                            | 2026-08-04                | notifications owner      | Platform PR not yet opened (as of 2026-08-04)                                                                                                                     | Terraform entry in `../vellum-assistant-platform/terraform/gcp/env/prod/vellum-assistant/main.tf` with `defaultEnabled: true` and `tags = ["assistant"]` provisioning the `desktop-presence-suppression` assistant flag, which suppresses the `chat.assistant_reply` push while a Mac desktop client reports itself attended. Declared on the presence-gated-reply-push feature branch rather than `main`, so this row runs ahead of the registry until that branch lands. Needed as a remote kill switch: until it exists the flag resolves to the registry default (on), and the only way to restore unconditional reply pushes is a client-side override. |
| `experiment-balanced-model-2026-08-06`                    | 2026-08-06                | inference owner          | vellum-ai/vellum-assistant-platform#9830 (open, merges first)                                                                                                     | Terraform entry in `../vellum-assistant-platform/terraform/gcp/env/prod/vellum-assistant/main.tf` with `tags = ["assistant"]` provisioning the multivariate `experiment-balanced-model-2026-08-06` flag with string variations `control` / `terra` / `glm-5p2` and default `control`. Repoints the model the managed Balanced inference profile resolves to (`gpt-5.6-luna` / `gpt-5.6-terra` / `accounts/fireworks/models/glm-5p2`). Until the flag exists on the platform it resolves to the registry default (`control`), so no install moves off the shipped pin; rollout percentages are set in the LaunchDarkly dashboard, not here.                   |
| `acp-claude-oauth-connect`                                | 2026-07-15                | ACP owner                | Platform PR not yet opened (as of 2026-07-15)                                                                                                                     | Terraform entry in `../vellum-assistant-platform/terraform/gcp/env/prod/vellum-assistant/main.tf` with `defaultEnabled: false` and `tags = ["assistant"]` provisioning the `acp-claude-oauth-connect` assistant flag, which gates the in-app Connect Claude Code OAuth flow. Declared on the ACP OAuth connect feature branch (#38156) rather than `main`, so this row runs ahead of the registry until that branch lands. Needed so managed assistants can receive a remote override; until then the flag resolves to the registry default (off).                                                                                                           |
| `web-remote-ingress` (scope change)                       | 2026-08-17                | remote web owner         | Platform PR not yet opened (as of 2026-08-17)                                                                                                                     | Retag the existing `web-remote-ingress` flag in `../vellum-assistant-platform/terraform/gcp/env/prod/vellum-assistant/main.tf` from `tags = ["assistant"]` to `tags = ["client"]` so it syncs to client-scope consumers; the registry scope changed `assistant` → `client` and the flag now only shows/hides the pair-a-device settings card.                                                                                                                                                                                                                                                                                                                |
| `paired-devices-ui`                                       | 2026-08-19                | remote web owner         | Platform PR not yet opened (as of 2026-08-19)                                                                                                                     | Terraform entry in `../vellum-assistant-platform/terraform/gcp/env/prod/vellum-assistant/main.tf` with `defaultEnabled: false` and `tags = ["client"]` provisioning the client-scoped `paired-devices-ui` flag, which shows/hides the paired-devices list + revoke section inside the pair-a-device settings card. Visibility only; host-side revocation stays available via `vellum devices`.                                                                                                                                                                                                                                                                 |
| `web-presence-suppression`                                | 2026-08-20                | notifications owner      | Platform PR not yet opened (as of 2026-08-20)                                                                                                                     | Terraform entry in `../vellum-assistant-platform/terraform/gcp/env/prod/vellum-assistant/main.tf` with `defaultEnabled: true` and `tags = ["assistant"]` provisioning the `web-presence-suppression` assistant flag, which suppresses the `chat.assistant_reply` push while a browser tab reports itself visible on that conversation. Declared on the web-presence feature branch rather than `main`, so this row runs ahead of the registry until that branch lands. Needed as a remote kill switch: until it exists the flag resolves to the registry default (on), and the only way to restore unconditional reply pushes for web-visible conversations is a client-side override. |
| `obscure-credits`                                         | 2026-08-20                | billing plan owner       | Platform PR not yet opened (as of 2026-08-20)                                                                                                                     | Terraform entry in `../vellum-assistant-platform/terraform/gcp/env/prod/vellum-assistant/main.tf` with `defaultEnabled: false` and `tags = ["client"]` provisioning the client-scoped `obscure-credits` flag, which gates the billing Plan section's usage-balance bar (replacing the price footer) and the obscured credits spec chip. A companion platform branch `Jasonnnz/obscure-credits-flag-period-start` is being prepared in parallel and also adds `current_period_start` to the subscription response the bar reads. Until the flag exists on the platform it resolves to the registry default (off), so the Plan section keeps shipping today's price footer. |
