/**
 * Expected answer for the P&L fixture. The spreadsheet itself is stubbed
 * (see assets/STUB.md); when the fixture lands it must keep "Labor" as the
 * unambiguous largest spend category or this constant must change with it.
 */
export const LARGEST_SPEND_CATEGORY = "Labor";
