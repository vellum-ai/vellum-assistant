# Asset stub

Pending fixtures for this test (intentionally not generated yet while the
Evals CRM asset-handling decision is open — no >1MB assets in-repo):

- Pre-created interview transcript uploaded to the assistant before the
  conversation starts.
- Mock ATS with a candidate details page exposing empty form fields, plus an
  inspection hook so metrics can verify the fields were set appropriately.
