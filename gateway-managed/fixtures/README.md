# Managed Gateway Contract Fixtures

These fixtures are canonical examples for the public managed-gateway contracts in this directory.

Use them for compatibility checks only. Runtime behavior is implemented in `vellum-assistant-platform`.
