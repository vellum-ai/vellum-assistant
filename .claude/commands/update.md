# Update — Pull Latest and Restart

Pull the latest changes from main and install dependencies.

## Steps

1. Switch to main and pull latest:
   ```bash
   git checkout main
   git pull origin main
   ```

2. Install dependencies (follow the project conventions in CLAUDE.md for the correct install command).

3. Report what was pulled (new commits) and confirm the project is ready.
