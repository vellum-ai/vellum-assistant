{
  "name": "@vellumai/simple-memory",
  "version": "0.0.1",
  "description": "Phase 0 experimental memory plugin. Scaffolds the hooks (memoryRetrieval, injector, persistence observer, remember/recall tools, init/shutdown) a memory system would need against the agent-plugin-system contract — trivial in-memory implementation so the wires can be exercised end-to-end before the real logic moves into the harness.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "./register.ts",
  "engines": {
    "node": ">=20.12.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.10",
    "@types/node": "25.5.0",
    "typescript": "5.9.3"
  }
}
