name: PR Chrome Extension Checks

on:
  pull_request:
    types: [synchronize, opened, reopened]
    paths:
      - 'clients/chrome-extension/**'
      - '.github/workflows/pr-chrome-extension.yaml'

concurrency:
  group: pr-chrome-extension-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  checks:
    name: Lint, Type Check & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: clients/chrome-extension

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.9'

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

      - name: Type check
        run: bun run typecheck

      - name: Test
        run: bun run test
