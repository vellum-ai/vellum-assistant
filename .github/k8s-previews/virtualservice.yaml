apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: vellum-assistant
  namespace: ${NAMESPACE}
spec:
  hosts:
    - "app.${NAMESPACE}.previews.vellum.ai"
  gateways:
    - istio-system/preview-gateway
  http:
    - route:
        - destination:
            host: vellum-assistant.${NAMESPACE}.svc.cluster.local
            port:
              number: 80
