apiVersion: v1
kind: Service
metadata:
  name: vellum-assistant
  namespace: ${NAMESPACE}
  labels:
    app: vellum-assistant
spec:
  selector:
    app: vellum-assistant
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
