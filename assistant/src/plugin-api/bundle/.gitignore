# Generated by `bun run build:plugin-api` (chained from `bun install`
# via the assistant's postinstall hook). The sibling `index.d.ts` is
# hand-maintained and committed; only the runtime artifact is ignored.
index.js
