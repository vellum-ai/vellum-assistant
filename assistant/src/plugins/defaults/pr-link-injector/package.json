{
  "name": "default-pr-link-injector",
  "version": "1.0.0",
  "description": "First-party default plugin that detects git push in bash tool calls, resolves the PR link, and injects it into the assistant's final reply if the model didn't already include it.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.12.0"
  },
  "peerDependencies": {
    "@vellumai/plugin-api": "^0.8.0"
  }
}
