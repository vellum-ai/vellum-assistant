apiVersion: v1
kind: Service
metadata:
  name: vellum-gateway
  labels:
    app: vellum-gateway
spec:
  type: ClusterIP
  selector:
    app: vellum-gateway
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
